# Local Delivery Agent configuration for PSFX Suite

# Mailbox quotas
protocol lda {
  mail_plugins = $mail_plugins quota
}

# Sieve filtering (future feature)
# plugin {
#   sieve = ~/.dovecot.sieve
#   sieve_default = /etc/dovecot/sieve/default.sieve
# }
